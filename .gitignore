*
!index.html
!README.md
!PUBLISH-STEPS.md
!.gitignore
!sw.js
!manifest.webmanifest
!*.png
!404.html
!.nojekyll
!LICENSE
!TERMS.md
!THIRD-PARTY-NOTICES.md
!sync.json
# v2.27 — the legal pack the app's own #/rights page links out to, and the one test suite that
# proves the privacy claims. These are published deliberately: a privacy notice nobody can reach,
# and an assertion nobody can read, are worth exactly nothing to the person assessing the app.
!docs/
!docs/*.md
!test/
!test/*.js
# v2.29 — published course packs. The index is generated by build.js from packs/, so both it and
# the pack files have to ship; the app fetches them from this same origin and from nowhere else.
!packs/
!packs/*.json
